# amis-helper v1.2 批次 2 评审报告

| 项 | 值 |
|---|---|
| 评审对象 | `amis-helper` v1.2 批次 2（pitfalls 引用化 + SKILL.md 索引化） |
| 审查文件 | `references/pitfalls.md` / `SKILL.md` |
| 评审基准 | `docs/plan-iteration.md`（§5.2 的 1.2.2 / 1.2.3 / 1.2.6、§4.4、§6.3） |
| 审查方式 | 全文通读 2 文件 + 5 权威文件 + `git diff` 核对改动范围/未动项 + 字节级 CRLF 检查 |
| 评审日期 | 2026-08-31 |
| 结论 | **放行**（阻塞级 0 项） |

---

## 0. 结论摘要

改动范围干净（`git diff --stat` 仅 2 文件）、ID 引用 16/16 真实可达、SSOT 无正确写法残留、编号连续无缺号、体量与 CRLF 全部达标、触发矩阵与 §6.3 逐字一致。**核心质量过硬，无阻塞项**。仅剩 1 处分组归类错位（建议级）与 3 处润色瑕疵。

## 1. 审查清单

```yaml
检查1_ID引用真实性: pass   # 16 条「见 X-xx」的文件/章节号/ID 逐条与权威定义核对一致
检查2_SSOT: pass           # pitfalls 无正确写法复述、无代码块；SKILL 索引表无细节复述
检查3_悬空引用: pass       # 无 self-check.md / examples/INDEX.md / crud-base.json
检查4_编号完整性: pass     # P-01~P-16 连续无缺号；旧 16 条（含 P1.8）全映射无遗漏
检查5_体量: pass           # SKILL.md 60 行（≤60）/ pitfalls.md 55 行（≤120）
检查6_CRLF: pass           # 两文件均 100% CRLF，bareLF=0
检查7_frontmatter与决策表: pass  # version 1.1.0 未动；§2 决策表 12 行未动（git diff 无该区改动）
检查8_触发矩阵: pass       # 6 行覆盖 5 reference + examples，关键词与 §6.3 一致，无 self-check 行
```

## 2. 问题分级清单

**阻塞级：0 项。**

**建议级（P1，不阻塞）**

| # | 文件:行 | 问题 | 建议 |
|---|---|---|---|
| 1 | pitfalls.md:31-55 | 分组归类错位：P-13（每页切换器→C-01）、P-14（统计条→C-04）属 **crud 域**，P-16（JSON 注释→R-01）属**通用域**，却全被归入 `## 表单控件`。原 5 分组（弹层/select/分页/布局/格式）压缩成 2 组时，P3/P4/P5 内容被错误并入 | 拆出「CRUD/列表」「通用」分组，或按触发矩阵 5 域对齐分组标题 |

**润色级（P2，不影响正确性）**

| # | 文件:行 | 问题 | 建议 |
|---|---|---|---|
| 2 | pitfalls.md:49 | P-14 格式未统一：用 `症状：…→ 解法见…`，其余条目为 `错误：…→ 见…`，且「解法见」措辞不统一 | 统一为「错误：用 statistics → 见…」或保留症状但措辞对齐「见」 |
| 3 | SKILL.md:20-21 | 索引表行 20/21（D-01/D-04/D-06、D-02/D-08 聚合行）一句话超 25 字，与声称「≤25字」不符 | 聚合行可接受；若要严格达标，拆为单 ID 行 |
| 4 | pitfalls.md:46 + crud.md:24 | P-13 丢失「`perPageOptions` 是错误属性名」这一具体反例（旧 P3.1 有，C-01 元数据也未提及） | 可在 C-01 后果或 P-13 补一句「perPageOptions 非合法属性」 |

**非本批次（提示，不阻断）**

| # | 文件:行 | 问题 |
|---|---|---|
| 5 | META.md:55 | 「P-01～P-16 新编号批次 2 落地，**当前为 P1.x/P2.x 旧编号**」——批次 2 已落地后该句已过时，应同步为「已落地」。META.md 不在本批次改动范围，留待后续批次收口 |

## 3. 附注

- **迭代计划 §4.3 笔误**：映射示例表写「R9 统计条用 tpl → `C-02`」，实际 crud.md 中 C-02 是「id+name」、C-04 才是「统计条」。SKILL.md 索引表用 `C-04` 是**正确**的（与权威定义一致），后续读 §4.3 时勿被误导。
- **R4/R5 聚合行**：SKILL.md 将 R4→D-01/D-04/D-06、R5→D-02/D-08 聚合为一索引行，与声称映射一致，且一句话准确概括三者，SSOT 未破坏，属合理主题聚合。
